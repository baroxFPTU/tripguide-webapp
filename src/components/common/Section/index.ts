import { SectionHeading as Heading } from './SectionHeading';
import { SectionHeroHeading as HeroHeading } from './SectionHeroHeading';
import { SectionDescription as Description } from './SectionDescription';
import { SectionContent as Content } from './SectionContent';
export { default } from './component';

export const SectionItem = {
  Heading,
  HeroHeading,
  Description,
  Content,
};
