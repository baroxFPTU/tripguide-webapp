import { DropdownMenu as Menu } from './DropdownMenu';
import { DropdownLabel as Label } from './component';

export const DropdownItem = {
  Menu,
  Label,
};

export { default } from './component';
